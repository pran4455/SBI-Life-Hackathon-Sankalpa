<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify TOTP - Financial AI Hub</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            font-family: 'Segoe UI', sans-serif;
            margin: 0;
            padding: 0;
            background: linear-gradient(120deg, #6d3cc5, #ff69b4);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }

        .nav-bar {
            position: fixed;
            top: 0;
            width: 100%;
            height: 60px;
            background-color: rgba(0, 0, 0, 0.4);
            display: flex;
            align-items: center;
            padding: 0 20px;
            z-index: 1000;
        }

        .nav-bar img {
            height: 40px;
        }

        .container {
            background-color: #fff;
            padding: 25px 30px;
            border-radius: 15px;
            box-shadow: 0 8px 24px rgba(0,0,0,0.2);
            width: 90%;
            max-width: 360px;
            text-align: center;
            margin-top: 60px;
        }

        .title {
            color: #6d3cc5;
            font-size: 22px;
            margin-bottom: 10px;
        }

        .description {
            color: #444;
            font-size: 15px;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 20px;
            text-align: left;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: #333;
        }

        input {
            width: 100%;
            padding: 12px;
            font-size: 18px;
            border-radius: 8px;
            border: 1px solid #ccc;
            text-align: center;
            letter-spacing: 4px;
            font-weight: bold;
        }

        input:focus {
            outline: none;
            border-color: #6d3cc5;
            box-shadow: 0 0 5px rgba(109, 60, 197, 0.3);
        }

        .btn {
            background-color: #6d3cc5;
            color: white;
            border: none;
            padding: 12px;
            width: 100%;
            font-size: 15px;
            border-radius: 8px;
            cursor: pointer;
            margin-top: 10px;
        }

        .btn:hover {
            background-color: #552ba0;
        }

        .error {
            color: #e74c3c;
            background: #fdecea;
            padding: 10px;
            margin-top: 15px;
            border-radius: 8px;
            font-size: 14px;
        }

        .footer {
            position: fixed;
            bottom: 0;
            width: 100%;
            background-color: rgba(0, 0, 0, 0.3);
            color: white;
            text-align: center;
            padding: 10px 0;
            font-size: 13px;
        }
    </style>
</head>
<body>

    <div class="nav-bar">
        <img src="/static/sankalpa.png" alt="Logo">
    </div>

    <div class="container">
        <h1 class="title">Two-Factor Authentication</h1>
        <p class="description">Enter the 6-digit code generated by your authenticator app.</p>

        <form action="/verify_totp" method="post">
            <div class="form-group">
                <label for="totp">Authentication Code</label>
                <input type="text" id="totp" name="totp" required maxlength="6" autocomplete="off" placeholder="000000">
            </div>
            <button type="submit" class="btn">Verify</button>
        </form>

        <% if (locals.error) { %>
            <p class="error"><%= error %></p>
        <% } %>

        <br>

        <div class="actions">
            <a href="/generate_qr_code" class="btn btn-secondary" role="button">Get QR Code</a>            
        </div>
    </div>

    <div class="footer">
        &copy; 2025 Financial AI Hub. All rights reserved.
    </div>

</body>
</html>
